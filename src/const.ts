export const IMAGE_URL = "https://cdn.artisant.io/api/files/630x819/";

export enum ApiRoute {
  Products = "/products",
}

export enum AppRoute {
  Products = "products",
}

export enum ReducerNameSpace {
  data = "DATA",
}

export enum SortType {
  Quantity = "Quantity",
  Popular = "Popular",
}
